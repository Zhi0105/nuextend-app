import { Text, View } from "react-native"

export const Dashboard = () => {
    return (
        <View className="dashboard-main min-h-screen flex justify-center items-center">
            <Text>Hello from dashboard</Text>
        </View>
    )
}
